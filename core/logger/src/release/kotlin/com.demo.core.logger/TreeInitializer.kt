package com.demo.core.logger

import android.content.Context

/**
 * [Release] Provides a no-op implementation for tree initialization.
 */
internal fun initializeTrees(config: LogConfig) {
    // No-op in release builds.
}